<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mypage">

	<select id="listHistory" parameterType="java.lang.Long" 
	resultType="visitvo">
		<![CDATA[
		SELECT a.no, b.name as program_name, a.reg_date, a.whitening_score, a.wrinkle_score, a.elastic_score, a.moisture_score, a.acne_score, a.average_score 
		FROM VISIT a, PROGRAM b
		WHERE a.user_no = #{userNo } AND a.program_no = b.no ORDER BY a.no
		]]>
	</select>
	
	
</mapper>